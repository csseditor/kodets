- if false # change to if lesson is completedlist-group-item-success
  %li.list-group-item.list-group-item-success
    = link_to item.name, item
    - if current_user.teacher?
      .pull-right
        = link_to 'Edit', edit_code_lesson_path(item)
- else
  %li.list-group-item
    = link_to item.name, item
    - if current_user.teacher?
      .pull-right
        = link_to 'Edit', edit_code_lesson_path(item)
